{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "vision": {
      "model": "opencode/minimax-m3-free",
      "mode": "subagent",
      "description": "Use when the main model needs to analyze images or screenshots (e.g., from Playwright, browser screenshots, or uploaded images). Has vision capabilities.",
      "permission": {
        "read": "allow",
        "bash": "ask"
      }
    }
  },
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp"],
      "enabled": true
    }
  }
}
